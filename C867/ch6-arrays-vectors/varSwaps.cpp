#include <iostream>
using namespace std;

int main() {
    int x = 10, y = 20, tmp;

    // Generally, this can be used to swap values of two variables.
    tmp = x;
    x = y;
    y = tmp;

    return 0;
}